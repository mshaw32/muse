{
  "name": "@muse/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-cutting types, settings, and logging shared across the MUSE backend, services, and Electron layers.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
