{
  "name": "muse",
  "version": "0.2.0",
  "private": true,
  "description": "MUSE (Michael's Unified Strategy Engine) — a Jarvis-style personal AI operating system.",
  "workspaces": [
    "shared",
    "services",
    "backend",
    "frontend",
    "electron"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "backend": "npm run build:libs && npm run dev --workspace=backend",
    "build:shared": "npm run build --workspace=shared",
    "build:services": "npm run build --workspace=services",
    "build:libs": "npm run build:shared && npm run build:services",
    "electron": "npm run build:libs && npm run build --workspace=electron && npm run start --workspace=electron",
    "dev:all": "npm run build:libs && concurrently -k -n BACKEND,FRONTEND -c blue,green \"npm run backend\" \"npm run dev\"",
    "verify:phase1": "bash ./scripts/verify-phase1.sh",
    "verify:phase2": "bash ./scripts/verify-phase2.sh",
    "verify:phase3": "bash ./scripts/verify-phase3.sh",
    "verify:phase4": "bash ./scripts/verify-phase4.sh",
    "verify:all": "bash ./scripts/verify-phase1.sh && bash ./scripts/verify-phase2.sh && bash ./scripts/verify-phase3.sh && bash ./scripts/verify-phase4.sh"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "typescript": "^5.6.3"
  }
}
